import { Roles } from "../register/roles";

export class Form {
    tel: string | null = "";
    country: string | null = "";
    role!: string;
    name: string |null = "";
    username: string | null = "";
    email: string | null = "";
    password:any;
}